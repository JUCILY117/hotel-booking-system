# ==========================
# Database connection (Prisma expects this in DATABASE_URL)
# ==========================
# PostgreSQL example (recommended for production):
#   postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=

# ==========================
# JWT secrets (for session/auth tokens)
# ==========================
# Used to sign and verify JWTs. Keep these secure.
JWT_SECRET=
JWT_REFRESH_SECRET=

# ==========================
# Admin seed credentials (optional)
# ==========================
# Used by the seed script to create an initial admin user.
# Can be left empty and created manually if preferred.
ADMIN_EMAIL=
ADMIN_PASSWORD=

# ==========================
# External API keys
# ==========================
# Unsplash API key for hotel images.
UNSPLASH_ACCESS_KEY=

# Gemini API key for AI features (if enabled).
# Leave blank if not using AI features.
GEMINI_API_KEY=

# ==========================
# Email service settings
# ==========================
MAIL_DRIVER=
MAIL_HOST=
MAIL_PORT=
MAIL_SECURE=
MAIL_USER=
MAIL_PASS=

# ==========================
# Client URL and Environment settings
# ==========================
CLIENT_URL= 
# The URL of your front-end app (used for JWT token verification or CORS).
NODE_ENV= 
# Set to "development" for local development or "production" for deployed environments.

# ==========================
# Quick troubleshooting/tips
# ==========================
# - If migrations fail: Ensure `DATABASE_URL` is correct and the DB user has permissions.
# - If authentication fails: Ensure `JWT_SECRET` is consistent across all instances.
# - For development: Use SQLite (`file:./dev.db`) for simplicity.
# - Run migration and seed commands after filling values:
#   npm run migrate    # To apply database migrations
#   npm run seed       # To seed initial data (if available)

# SECURITY REMINDERS
# - Never commit real secrets to git. Use environment-specific config management.
# - Rotate keys regularly and revoke compromised keys immediately.
# - Use strong, unique values for all secrets.