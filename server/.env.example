# Example .env for the Hotel Booking System
# ----------------------------------------
# Copy this file to `.env` and fill in the values below before starting the server.
# Do NOT commit your `.env` to version control. Keep secrets safe.

# ==========================
# Database connection (Prisma expects this in DATABASE_URL)
# ==========================
# PostgreSQL example (recommended for production):
#   postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Example local Postgres value:
#   postgresql://postgres:password@localhost:5432/hotel_db?schema=public
# SQLite example (good for quick local testing):
#   file:./dev.db
# If you use SQLite, set DATABASE_URL=file:./dev.db
DATABASE_URL=

# ==========================
# JWT secret (session/auth tokens)
# ==========================
# Used to sign JWTs. Use a long, random string (32+ bytes recommended).
# Generate one quickly with:
#   openssl rand -hex 32
# or with Node:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# ==========================
# Admin seed credentials (optional)
# ==========================
# These are used by the seed script (if present) to create an initial admin user.
# You can leave these empty and create an admin via the UI or CLI instead.
# If you set them, pick a secure password and change it after first login.
ADMIN_EMAIL=
ADMIN_PASSWORD=

# ==========================
# External API keys
# ==========================
# Unsplash (for sample hotel images). Get a free key at https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=

# Gemini / Google AI API key (if you enable AI features).
# If you are not using AI features, leave this blank.
# Obtain credentials from Google Cloud (AI Platform) or the provider used in your app.
GEMINI_API_KEY=

# ==========================
# Quick troubleshooting / tips
# ==========================
# - If migrations fail: verify `DATABASE_URL` points to a running DB and the user has
#   permission to create the schema / tables. For Postgres ensure the database exists.
# - If authentication fails: confirm `JWT_SECRET` is set to the same value for all
#   running instances (regenerating the secret will invalidate existing tokens).
# - For development: use SQLite (`file:./dev.db`) to avoid installing a DB server.
# - After filling values, run your usual seed/migrate commands, for example:
#   npm run migrate    # (or the project's migration command)
#   npm run seed       # (if available in this repo)

# SECURITY REMINDERS
# - Never commit real secrets to git. Use environment-specific config management
#   (e.g., Docker secrets, Vault, CI/CD encrypted variables) for production.
# - Rotate keys regularly and revoke compromised keys immediately.
